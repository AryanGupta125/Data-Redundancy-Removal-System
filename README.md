# CodeAlpha_Data-Redundancy-Removal-System
A Python + Flask system that validates incoming data entries using SHA-256 hashing and fuzzy matching to block duplicates and near-duplicates, ensuring only clean, unique records are stored in the database.
